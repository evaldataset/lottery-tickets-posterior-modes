# CIFAR-100 Dataset-Generality Cell

This generated table reports the seed-level outcome of the mode/ticket
probe on CIFAR-100 ResNet-20. It tests whether the posterior-mode
failure documented on CIFAR-10 ResNet-20 is an artifact of the CIFAR-10
dataset.

- Source run: `runs/cifar100_resnet20_long30_rewind1_mode_ticket_distribution_sgld_r5_p0p3/20260524_225916`
- Dataset/model: `cifar100` / `resnet20`
- Seeds: `[0, 1, 2, 3, 4]`
- IMP rounds: 5; prune fraction 0.3
- Posterior sampler: `sgld` with 10 samples per seed
- Evaluation split: `test`

| Quantity | Five-seed value |
| --- | --- |
| Dense chain-start accuracy | 0.6296 (95% CI [0.6249, 0.6344]) |
| Matched-sparsity IMP accuracy | 0.6356 (95% CI [0.6318, 0.6393]) |
| IMP sparsity | 0.8319 (95% CI [0.8319, 0.8319]) |
| Posterior SGLD sample accuracy | 0.6169 (95% CI [0.6131, 0.6208]) |
| Posterior-to-chain-start mask Hamming | 0.0367 (95% CI [0.0363, 0.0370]) |

## Seed-Level Direction Checks

| Check | Seeds |
| --- | ---: |
| IMP accuracy > dense chain start | 5/5 |
| Posterior sample accuracy < dense chain start | 5/5 |
| Posterior sample accuracy < IMP accuracy | 5/5 |

Interpretation:

- The lottery-ticket effect survives on CIFAR-100: IMP beats the dense
  chain start in every seed.
- Posterior SGLD samples stay below both the dense chain start and the
  IMP ticket in every seed, so the posterior does not recover ticket
  performance.
- Posterior TopK masks track the chain-start support: the
  posterior-to-chain-start Hamming distance is small and tight across
  seeds, the same regime observed on CIFAR-10 ResNet-20.

Dataset generality of the posterior-mode failure: **holds**.

This file is generated by
`scripts/summarize_cifar100_generality_probe.py`.
